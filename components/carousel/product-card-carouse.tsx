import { WooImage } from '@/types/product.types'

type ProductCardCarouselProps = {
  images: WooImage[]
  title: string
}
const ProductCardCarousel = () => {
  return <div>ProductCardCarousel</div>
}

export default ProductCardCarousel
